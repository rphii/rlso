sources = [
  'so-cmp.c',
  'so-heap.c',
  'so.c'
  ]

headers = [
  'endian.h',
  'so-cmp.h',
  'so-heap.h',
  'so.h'
  ]

install_headers(headers, subdir: 'rphii')
rphiiso = library('rphiiso',
  sources,
  install: true,
  )


